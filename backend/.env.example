# Supabase Configuration (Required)
# Get these from your Supabase Project Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Google Gemini AI (Required for AI features)
# Get API Key from https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key

# Google OAuth (Optional - for Calendar/Gmail integration)
# Create credentials in Google Cloud Console > APIs & Services > Credentials
# Authorized Redirect URI should be: http://localhost:3000 (frontend) or your production URL
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/
